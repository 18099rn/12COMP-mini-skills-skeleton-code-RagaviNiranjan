/********************************************************/
//
/********************************************************/
function setup() {
  createCanvas(400, 400);
}

function draw() {

  
  strokeWeight(2);
  stroke(0,255,0,50);
  line(mouseX, mouseY, mouseX+5, mouseY+5);
 
}